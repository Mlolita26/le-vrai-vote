// URL du service « Communauté » (Cloudflare Worker) — voir worker/README.md.
// Tant que c'est vide, les ampoules s'affichent en mode « bientôt » (sans
// compteur) et le classement indique que la fonctionnalité s'active.
window.LVV_API = "";
